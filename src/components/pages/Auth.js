import Login from '../partials/Login';
import Signup from '../partials/Signup';

const Auth = (props) => {
    return (
        <div>
            <Signup />
            <Login />
        </div>
    );
}

export default Auth;